alias: Master clost motion lights
trigger:
  platform: state
  entity_id: binary_sensor.closet_motion
  from: 'off'
  to: 'on'
condition:
  condition: template
  value_template: '{{ states.input_select.mode.state != "Pause" }}'
action:
  service: light.turn_on
  entity_id: light.master_closet
