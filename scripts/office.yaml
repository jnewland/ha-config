sequence:
  - service: homeassistant.turn_on
    data_template:
      entity_id: >
        {%- if states.input_select.mode.state == "Movie" -%}
        script.mode_movie
        {%- else -%}
        script.working
        {%- endif -%}
