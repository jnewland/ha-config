sequence:
- service: light.turn_on
  data_template:
    entity_id: master_toilet_62
    brightness: |
      {{ brightness | default(states('input_number.brightness') | int) }}
- condition: template
  value_template: >
    {{ states.input_select.mode.state != "Sleeping"  }}
- service: light.turn_on
  data_template:
    entity_id:
    - light.master_bath_overhead_36
    - light.master_shower_34
    - light.master_mirror_35
    brightness: |
      {{ brightness | default(states('input_number.brightness') | int) }}
