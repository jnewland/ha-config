sequence:
- service: script.duck_light
  data_template:
    entity_id: |
     {% for entity_id in states.group.alert_lights.attributes.entity_id if is_state(entity_id, 'on') -%}
     {{ entity_id }}{% if not loop.last %}{{ "," }}{% endif %}
     {%- endfor %}
