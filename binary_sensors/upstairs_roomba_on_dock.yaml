platform: template
sensors:
  upstairs_roomba_on_dock:
    value_template: |
      {{ (states.sensor.upstairs_roomba_state.state | default(None)) in [
      'st_base',
      'st_base_recon',
      'st_base_full',
      'st_base_trickle',
      'st_base_wait'
      ] }}
