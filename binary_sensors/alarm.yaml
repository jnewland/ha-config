platform: template
sensors:
  entry_door:
    value_template: |
      {{ "Entry Door is Open" in (states.alarm_control_panel.alarm.attributes.sensor_status | default("")) }}
  master_window:
    value_template: |
      {{ "Master is Open" in (states.alarm_control_panel.alarm.attributes.sensor_status | default("")) }}
  downstairs_bathroom_window:
    value_template: |
      {{ "Downstairs Bathroom is Open" in (states.alarm_control_panel.alarm.attributes.sensor_status | default("")) }}
  downstairs_window:
    value_template: |
      {{ "Downstairs Window is Open" in (states.alarm_control_panel.alarm.attributes.sensor_status | default("")) }}
  bedroom_two_window:
    value_template: |
      {{ "Bedroom Two is Open" in (states.alarm_control_panel.alarm.attributes.sensor_status | default("")) }}
  office_window:
    value_template: |
      {{ "Office Window is Open" in (states.alarm_control_panel.alarm.attributes.sensor_status | default("")) }}
  living_room_motion:
    friendly_name: living room motion
    value_template: |
      {{ "Motion Detector is Activated" in (states.alarm_control_panel.alarm.attributes.sensor_status | default("")) }}
