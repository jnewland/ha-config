platform: template
sensors:
  together:
    entity_id:
    - device_tracker.katies_iphone
    - device_tracker.jphone_x
    value_template: |
      {{
        states.device_tracker.katies_iphone != None and
        states.device_tracker.jphone_x != None and
        distance(states.device_tracker.jphone_x, states.device_tracker.katies_iphone) != None and
        distance(states.device_tracker.jphone_x, states.device_tracker.katies_iphone) < 0.25
      }}
