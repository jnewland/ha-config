platform: rest
name: Roomba downstairs bin full
resource: !env_var THINKING_CLEANER_FULL_STATUS_URL
value_template: |
  {%- if value_json -%}
  {{ value_json.tc_status.bin_status | int > 0 }}
  {%- endif -%}
