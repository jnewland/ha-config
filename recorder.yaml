purge_keep_days: 2
db_url: !env_var DB_URL
exclude:
  domains:
  - automation
  - updater
  - group
  - vacuum
  - counter
  entities:
  - sun.sun
  event_types:
    - call_service # Don't record service calls
